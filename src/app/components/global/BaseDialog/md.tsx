const Doc = () => (
  <div className="space-y-2 text-sm text-slate-700">
    <p>BaseDialog 相关组件封装在目录 <code>src/app/components/global/BaseDialog</code>。</p>
    <p>示例使用 <code>DialogFrame</code> 作为外层容器，可按需组合内容。</p>
  </div>
);

export default Doc;
